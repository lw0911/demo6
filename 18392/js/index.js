// JavaScript Document
var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(23);//截止当天小时
	startTime.setMinutes(40);//截止当天分钟
	startTime.setSeconds(40);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime001=startTime.getTime();
	function GetRTime001(){
	  var NowTime = new Date();
	  var nMS = EndTime001 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday001").hide();
		$("#countend001").show();
	  }else{
		 $("#coutnday001").show();
		 $("#countend001").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH001").text(nH);
		 $("#RemainM001").text(nM);
		 $("#RemainS001").text(nS); 
	  }
	}


var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(23);//截止当天小时
	startTime.setMinutes(33);//截止当天分钟
	startTime.setSeconds(40);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime002=startTime.getTime();
	function GetRTime002(){
	  var NowTime = new Date();
	  var nMS = EndTime002 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday002").hide();
		$("#countend002").show();
	  }else{
		 $("#coutnday002").show();
		 $("#countend002").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH002").text(nH);
		 $("#RemainM002").text(nM);
		 $("#RemainS002").text(nS); 
	  }
	}

	var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(12);//截止当天小时
	startTime.setMinutes(34);//截止当天分钟
	startTime.setSeconds(40);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime003=startTime.getTime();
	function GetRTime003(){
	  var NowTime = new Date();
	  var nMS = EndTime003 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday003").hide();
		$("#countend003").show();
	  }else{
		 $("#coutnday003").show();
		 $("#countend003").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH003").text(nH);
		 $("#RemainM003").text(nM);
		 $("#RemainS003").text(nS); 
	  }
	}

	var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(21);//截止当天小时
	startTime.setMinutes(24);//截止当天分钟
	startTime.setSeconds(40);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime004=startTime.getTime();
	function GetRTime004(){
	  var NowTime = new Date();
	  var nMS = EndTime004 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday004").hide();
		$("#countend004").show();
	  }else{
		 $("#coutnday004").show();
		 $("#countend004").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH004").text(nH);
		 $("#RemainM004").text(nM);
		 $("#RemainS004").text(nS); 
	  }
	}

		var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(19);//截止当天小时
	startTime.setMinutes(55);//截止当天分钟
	startTime.setSeconds(4);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime005=startTime.getTime();
	function GetRTime005(){
	  var NowTime = new Date();
	  var nMS = EndTime005 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday005").hide();
		$("#countend005").show();
	  }else{
		 $("#coutnday005").show();
		 $("#countend005").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH005").text(nH);
		 $("#RemainM005").text(nM);
		 $("#RemainS005").text(nS); 
	  }
	}

		var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(20);//截止当天小时
	startTime.setMinutes(3);//截止当天分钟
	startTime.setSeconds(4);//截止当天秒
	startTime.setMilliseconds(999);
	var EndTime006=startTime.getTime();
	function GetRTime006(){
	  var NowTime = new Date();
	  var nMS = EndTime006 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#coutnday006").hide();
		$("#countend006").show();
	  }else{
		 $("#coutnday006").show();
		 $("#countend006").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#RemainH006").text(nH);
		 $("#RemainM006").text(nM);
		 $("#RemainS006").text(nS); 
	  }
	}

/*限时秒杀*/

	var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(55);//截止当天小时
	startTime.setMinutes(20);//截止当天分钟
	startTime.setSeconds(20);//截止当天秒
	startTime.setMilliseconds(999);
	var htEndTime001=startTime.getTime();
	function htGetRTime001(){
	  var NowTime = new Date();
	  var nMS = htEndTime001 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#htcoutnday001").hide();
		$("#htcountend001").show();
	  }else{
		 $("#htcoutnday001").show();
		 $("#htcountend001").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#htRemainH001").text(nH);
		 $("#htRemainM001").text(nM);
		 $("#htRemainS001").text(nS); 
	  }
	}

	var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(22);//截止当天小时
	startTime.setMinutes(20);//截止当天分钟
	startTime.setSeconds(20);//截止当天秒
	startTime.setMilliseconds(999);
	var htEndTime002=startTime.getTime();
	function htGetRTime002(){
	  var NowTime = new Date();
	  var nMS = htEndTime002 - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#htcoutnday002").hide();
		$("#htcountend002").show();
	  }else{
		 $("#htcoutnday002").show();
		 $("#htcountend002").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#htRemainH002").text(nH);
		 $("#htRemainM002").text(nM);
		 $("#htRemainS002").text(nS); 
	  }
	}


/**[[限时抢购区专栏倒计时**/

$(function(){
	countDown("2014/12/12 22:10:59",null,"#areacdownTime .hour","#areacdownTime .minute","#areacdownTime .second");
});

function countDown(time,day_elem,hour_elem,minute_elem,second_elem){
	//if(typeof end_time == "string")
	var end_time = new Date(time).getTime(),//月份是实际月份-1
	
	//current_time = new Date().getTime(),
	sys_second = (end_time-new Date().getTime())/1000;
	var timer = setInterval(function(){
		if (sys_second > 0) {
			sys_second -= 1;
			var day = Math.floor((sys_second / 3600) / 24);
			var hour = Math.floor((sys_second / 3600) % 24);
			var minute = Math.floor((sys_second / 60) % 60);
			var second = Math.floor(sys_second % 60);
			day_elem && $(day_elem).text(day);//计算天
			$(hour_elem).text(hour<10?"0"+hour:hour);//计算小时
			$(minute_elem).text(minute<10?"0"+minute:minute);//计算分
			$(second_elem).text(second<10?"0"+second:second);// 计算秒
		} else { 
			clearInterval(timer);
			$('#areacdownTime').css({'background':'none','margin':'0'});
		}
	}, 1000);
}

/*--限时抢购区专栏倒计时]]--*/

/**[[爆款热卖区栏倒计时**/

$(function(){
	countDown("2014/11/11 11:10:59",null,"#hotdownTime .hour","#hotdownTime .minute","#hotdownTime .second");
});

function countDown(time,day_elem,hour_elem,minute_elem,second_elem){
	//if(typeof end_time == "string")
	var end_time = new Date(time).getTime(),//月份是实际月份-1
	
	//current_time = new Date().getTime(),
	sys_second = (end_time-new Date().getTime())/1000;
	var timer = setInterval(function(){
		if (sys_second > 0) {
			sys_second -= 1;
			var day = Math.floor((sys_second / 3600) / 24);
			var hour = Math.floor((sys_second / 3600) % 24);
			var minute = Math.floor((sys_second / 60) % 60);
			var second = Math.floor(sys_second % 60);
			day_elem && $(day_elem).text(day);//计算天
			$(hour_elem).text(hour<10?"0"+hour:hour);//计算小时
			$(minute_elem).text(minute<10?"0"+minute:minute);//计算分
			$(second_elem).text(second<10?"0"+second:second);// 计算秒
		} else { 
			clearInterval(timer);
			$('#hotdownTime').css({'background':'none','margin':'0'});
		}
	}, 1000);
}

/*--爆款热卖区栏倒计时]]--*/



/**[[十元特价尝鲜 倒计时**/

var startTime = new Date();
	//规定截止时间
	startTime.setFullYear(2014);//截止年份
	startTime.setHours(19);//截止当天小时
	startTime.setMinutes(1);//截止当天分钟
	startTime.setSeconds(40);//截止当天秒
	startTime.setMilliseconds(999);
	var tenEndTime=startTime.getTime();
	function tenGetRTime(){
	  var NowTime = new Date();
	  var nMS = tenEndTime - NowTime.getTime();
	  var nD = Math.floor(nMS/(1000 * 60 * 60 * 24));
	  var nH = Math.floor(nMS/(1000*60*60)) % 24;
	  var nM = Math.floor(nMS/(1000*60)) % 60;
	  var nS = Math.floor(nMS/1000) % 60;
	  if (nMS < 0){
		$("#tencoutnday").hide();
		$("#tencountend").show();
	  }else{
		 $("#tencoutnday").show();
		 $("#tencountend").hide();
		 // $("#RemainD").text(nD);//天数
		 $("#tenRemainH").text(nH);
		 $("#tenRemainM").text(nM);
		 $("#tenRemainS").text(nS); 
	  }
	}

/**十元特价尝鲜 倒计时]]**/

$(function() {
   $('.wzwplus').click(function(){
        var wzwval = $(this).parent().find('input').val();
        var wzwnum = parseInt(wzwval);
        if(typeof wzwnum == 'number'){
            var num = wzwnum + 1;
            $(this).parent().find('input').val(num);
        }
   });
   $('.wzwmint').click(function(){
        var wzwval = $(this).parent().find('input').val();
        var wzwnum = parseInt(wzwval);
        if(typeof wzwnum == 'number' && wzwnum > 1){
            var num = wzwnum - 1;
            $(this).parent().find('input').val(num);
        }
    });
})